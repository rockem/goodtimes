apply plugin: 'java'
apply plugin: 'idea'
apply plugin: 'jetty'
apply plugin: 'war'

repositories {
    mavenCentral()
}


task cucumber << {

}

evaluationDependsOn(':features')
evaluationDependsOn(':backend')

project(':backend').jettyRun.daemon = true
cucumber.dependsOn project(':backend').jettyRun

//cucumber.dependsOn jettyRun

project(':features').cucumber.mustRunAfter cucumber

dependencies {
    runtime project (':backend')
}


task wrapper(type: Wrapper) {
    gradleVersion = '2.2'
}